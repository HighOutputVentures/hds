import { FC } from 'react';
export interface TwitterIconProps {
    isDisabled?: boolean;
    variant?: 'outline' | 'solid';
}
declare const TwitterIcon: FC<TwitterIconProps>;
export default TwitterIcon;
