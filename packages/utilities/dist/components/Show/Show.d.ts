import * as React from 'react';
export declare type ShowProps = {
    when?: boolean;
    fallback?: JSX.Element | null;
};
export default function Show(props: React.PropsWithChildren<ShowProps>): any;
