describe("'runIfFn'", () => {
  it.todo('Returns function return value if arg 0 is function');

  it.todo('Returns value run if arg 0 is not a function');

  test.todo(
    'If arg 0 is function and arg 1 is given then arg 1 should be used as function params',
  );
});
